# Local Supabase: copy the values printed by `npm run db:start`.
SUPABASE_URL=###

# MUST be the Publishable (anon) key — NEVER the Secret (service_role) key.
# The Secret key bypasses Row-Level Security and would void every owner-isolation
# policy. See docs/reference/data-access.md.
SUPABASE_KEY=###
